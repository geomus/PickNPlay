<!-- HEAD -->
<%- include('./partials/head') %>
<!-- HEADER -->
<%- include('./partials/header') %>

  <main>
    <!-- carousel -->
    <div id="carouselCaptions" class="container p-0 carousel slide" data-ride="carousel">
      <ol class="carousel-indicators">
        <li data-target="#carouselCaptions" data-slide-to="0" class="active"></li>
        <li data-target="#carouselCaptions" data-slide-to="1"></li>
      </ol>
      <div class="carousel-inner">
        <div class="carousel-item active">
          <img src="../images/carousel1.jpg" id="carouselIMG" class="d-block w-100" alt="carousel 1">
          <div class="carousel-caption"
            style="color: black;background-color: rgba(255, 	255, 255, 0.6); border-radius: 5px;">
            <h5>Winter Sale!</h5>
            <p>Hacé click acá y mirá las nuevas ofertas de temporada.</p>
          </div>
        </div>
        <div class="carousel-item">
          <img src="/images/carousel2.jpg" id="carouselIMG" class="d-block w-100" alt="carousel 2">
          <div class="carousel-caption" style="color: white;background-color: rgba(0, 0, 0, 0.6); border-radius: 5px;">
            <h5>Visitá nuestras redes</h5>
            <p>No te pierdas ninguna novedad sobre nuestros productos.</p>
            <a href="https://www.facebook.com/" style="color: white;"><i class="fab fa-facebook-square p-2"></i></a>
            <a href="https://www.instagram.com/?hl=es-la" style="color: white;"><i
                class="fab fa-instagram-square p-2"></i></a>
          </div>
        </div>
      </div>
      <a class="carousel-control-prev" href="#carouselCaptions" role="button" data-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
      </a>
      <a class="carousel-control-next" href="#carouselCaptions" role="button" data-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
      </a>
    </div>
    <br><br>
    <!-- /carousel -->

    <!-- destacados -->
    <div class="container">
      <strong><h5 style="color: white; text-decoration: underline;"><i class="fas fa-fire-alt"></i> DESTACADOS :</h5></strong>
      <div class="row">
       <% let destacadosFinal = destacados.sort(() => Math.random() - Math.random()).slice(0, 4) %>
        <% for (producto of destacadosFinal){ let image = JSON.parse(producto.image)
          let preciofinal = puntoMil(producto.price-((producto.price*producto.discount)/100))
          let display = "";
          let tachado = "";
          if(producto.discount <= 0){
            display = "d-none"
            tachado = ""
          }else{
            display = "d-block text-right text-warning"
            tachado = "tachado"
          }%>
          <div class="col-12 col-sm-6 col-md-6 col-lg-3 py-2">
            <div class="card text-white bg-secondary mb-3 h-100">
              <a href="/products/<%= producto.id %>" style="color: black;">
                <img class="card-img-top img-fluid" src="/images/imgInstrumentos/<%= image[0] %>" alt="Producto destacado">
                <div class="card-body " style='height: 9.5rem;'>
                  <h4 class="card-title"><%= producto.name %></h4>
                  <h5 class="card-title text-right <%=tachado %>">$<%= puntoMil(Math.trunc(producto.price)) %></h5>
                  <h4 class="card-title  <%= display %>"><img src='/images/iconos/etiqueta-de-precio.svg' width='40'>&ensp; $<%=preciofinal%></h4>
                  </div>
              </a>
              <div class="card-footer bg-light">
                <a href='/products/list/<%=producto.category_id%>'><small class="text-muted text-capitalize"><%= producto.category.name %></small></a>
              </div>
            </div>
          </div>
        <% } %>
      </div>
    </div>
    <!-- /destacados -->
  </main>

<!-- FOOTER -->
<%- include('./partials/footer') %>